# Render API server — lightweight (512MB memory limit)

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0

# HTTP client (for Supabase REST + GitHub Actions dispatch)
requests>=2.31.0

# Data / Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Gmail API (for test-send endpoint)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
